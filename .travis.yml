sudo: false
language: node_js
node_js:
  - stable
install:
  - npm install
notifications:
  email: false
script:
  - 'npm run coverage'
after_success:
  - 'npm run coveralls'
services:
  - postgresql
before_script:
  - psql -c 'CREATE DATABASE elite;' -U postgres
